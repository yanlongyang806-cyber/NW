({
	debugLevel:                   5,
	useAuthLoginUrl:          false,        // If true, replace "gateway" with "auth" to make the auth url.
											// If false, uses the same url, with the https protocol.
})
