{
	// A is the bottom layer in PhotoShop
	float4 comp = %B% < 0.49999;
	float4 mult = 2 * %B% * %A%;
	float4 screen = 1 - 2 * (1 - %B%) * (1 - %A%);
	%Result%.xyz = lerp(screen, mult, comp).xyz;
	%Result%.w = 1;
}
